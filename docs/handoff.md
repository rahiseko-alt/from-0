# 引継ぎ

セッションをまたいで作業を継続するための文書です。役割は**「次にどの branch を見ればいいか」の
ポインタ**に限ります。branch 固有の詳細（何が途中か、次に何をするか）はその branch 自身の
コミットや開いてある PR の本文に書き、ここには短いポインタだけを置いてください。恒久的な
リポジトリのルールもここには書きません（`AGENTS.md`・`docs/decisions.md` を参照）。

同時に複数 branch が進行中の場合は、「次にやること」に branch ごと1エントリで列挙してください。
この雛形は複数 branch の並行開発を想定した状態管理は持っていません。本格的に必要になったら
`docs/decisions.md`「3-b. セッション間の引継ぎ」を読んでから設計し直してください。

- `AGENTS.md` がこのファイルを `@` で import しているため、セッション開始時に自動で読み込まれます
- 未記録の変更が残っていると、`.claude/hooks/handoff-check.sh` が1セッションに1回だけ更新を求めます
- セッション終了時、`.claude/hooks/handoff-stamp.sh` が末尾の状態を機械的に**上書き**します（追記ではない）
- 作業開始時は `/checkin`、区切りでは `/handoff`、終了時は `/checkout` を実行してください

**このファイル（引継ぎ文）を更新したら、必ず `main` へマージしてください。** 次のセッションは
`main` を新規クローンし、この docs/handoff.md しか自動では読みません。実際のコード変更は、
未完了なら push 済みの branch/worktree に残したままで構いません（push していれば origin に
残るため消えません）。

---

## いま何をしているか

**5手段（並列ネイティブ／機械検証／進捗管理／放置と振り返り／非エンジニア向け説明ルール）
の設計討議が完了し、実装方針が確定した。次は実装フェーズ。**

前回セッションの「配線マップ」（Artifact）を土台に、①〜⑤を1つずつユーザーと認識合わせした
結果、ユーザーから方針が出た：**「公式にあるならそれを使う、無いなら自作する」**。これを
受けて Claude Code 公式ドキュメントを一次情報で調べ直し（`docs/decisions.md`「14.」）、
手段単位ではなく**部品単位**で採否を確定した（同「15.」）。

- **公式機能を使う**: ①実行部分（worktree/`Workflow`の`parallel()`）／②実装中の自動継続
  （`/goal`）／④検出と通知（Routine）／⑤質問の出し方（`AskUserQuestion`、既に使用中）
- **自作を継続する**: ①並列可否の判定（`canRunInParallel`）／②`/checkout`時点の独立完了
  判定（`completion-checker`）と`verify`の中身検査／③進捗管理全般／④台帳に書く行動の強制／
  ⑤説明ルールを強制する仕組み

**次セッションは実装に入ってよい。** 何を作るかは `docs/decisions.md`「15.」の表がそのまま
作業リスト。旧「13.」の A/B/C 分類はこの「15.」に置き換わったので、そちらは参照しなくてよい
（「13.」自体は監査の一次記録として残す）。

- Artifact「配線マップ」URL（今回は更新していない）: https://claude.ai/code/artifact/6f820377-ee82-4b17-b395-3c1c9881ba79

## 完了したこと

`main` は `27fcc92`。今回のセッションでのコード変更・PR は無し（`docs/decisions.md`・
`docs/handoff.md` の更新のみ）。作業ブランチ `claude/checkin-j8rytv` に2コミット
push 済み（`bc971be` 下調べ記録、`2a26f5b` 採否判断）。

- **①並列ネイティブの認識合わせが完了**（前回）— 「レーン数は上物によって最適化される」で
  合意
- **公式機能の下調べを実施**（今回）— `/goal`・worktree・`Workflow`・Task tools・Routine・
  `AskUserQuestion` を一次情報（code.claude.com）で確認し、`docs/decisions.md`「14.」に記録。
  **前回セッションが「①の並列実行の仕組みが存在しない」と判定したのは誤りで、雛形が
  使っていないだけで Claude Code 本体には存在した**（同「14.」末尾に訂正を記録）
- **5手段すべての採否を部品単位で確定**（今回）— `docs/decisions.md`「15.」
- **雛形の目的を言語化し、実装を全数監査した**（数回前）— `docs/decisions.md`「13.」
- **PR #35 を revert した**（PR #36）— `/project-init` が `AGENTS.md`（土台の中心）を
  書き換える設計で、「上物で土台が変わってはならない」に違反していた。
  **同じ過ちを繰り返さないこと**
- **全体計画の仕組み**（PR #33）— `src/plan.ts` 等。`validatePlan` 以外は呼び出し元が無い
  状態のまま（「15.」で③は自作継続と確定したので、この呼び出し口作りが次の実装対象）

## 次にやること

**`docs/decisions.md`「15.」の表から1件選んで実装する。1セッション1件。**

- 公式機能を使う行（①②④⑤の一部）は、既存のツール・コマンドを呼ぶ配線を書くだけ
- 自作を継続する行のうち、非エンジニアへの効果が一番大きいのは**③進捗管理**
  （`countProgress`/`nextItem`/`nextId` の呼び出し口。現状、非エンジニアが現在地を知る
  手段が無い）
- ②`completion-checker`の呼び出しきっかけ、`verify`の中身検査の強化も未着手
- ①`canRunInParallel`の呼び出し口、④台帳に書く行動の強制、⑤説明ルールの強制も未着手
- どれも `docs/plan.json` が無いと動作確認しづらいものがある。**このリポジトリ自体には
  `docs/plan.json` を作っていない**（`from-0` は雛形そのものであり、`/plan-init` は
  雛形を使う側のプロジェクトで実行するもの）。実装時にテスト用の `docs/plan.json` が
  必要なら、テスト後に削除するか `.gitignore` 対象にすること

### 以前からの積み残し

- GitHub Ruleset の実態は `[曖昧]`。ユーザーが Settings → Rules → Rulesets で確認してほしい
- `handoff-stamp.sh`（SessionEnd）の実発火は未確認（優先度は低い）

## 注意点

**恒久的なリポジトリのルールはここに書かない。** `AGENTS.md`（指示）と `docs/decisions.md`
（根拠）を参照する。ここに書くのは、セッションをまたいで再発しうる作業上の落とし穴だけ。

- **土台（`AGENTS.md`・`.claude/` 配下）をプロジェクトごとに書き換える設計にしないこと。**
  PR #35 がこれで revert になった
- **「部品が無い」と「雛形が使っていない」を混同しない。** 公式機能の有無は一次情報
  （code.claude.com）で確認してから判定する。前回セッションはこれを怠って誤った
- **下調べの成果は新しいファイルを作らず `docs/decisions.md` に追記する**（ユーザー指示）。
  今回「14.」「15.」として追記した。今後も同様にすること
- **非エンジニアに答えられない質問をしない。** 何が起きているか・推奨はどれか・選ぶと
  どうなるかを平易に書いてから、選択肢で聞く
- **設計討議だけで終わったセッションでも、合意内容を `docs/decisions.md` に記録してから
  チェックアウトする**
- **mermaid で部品を全部1枚の図に詰め込むと、コンテナ幅に収めるため縮小されて読めなくなる。**
  上位の図はノード数を絞った要約に留め、個別の部品名・根拠は別のHTMLテーブルに分離すること
- **複数AIが同時に計画ファイルを編集するときの排他制御は、一次情報に記述がない `[曖昧]`。**
  再調査は不要
- 公式の `settings` ページだけは全文を読めていない（サイズ超過）

<!-- session-end-stamp -->

## セッション終了時点の状態（自動記録）

- 記録時刻: 2026-08-31 05:16 UTC
- ブランチ: `claude/checkin-j8rytv`
- HEAD: `bc971be`
- 未コミットの変更: なし
