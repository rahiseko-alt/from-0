# 引継ぎ

セッションをまたいで作業を継続するための文書です。役割は**「次にどの branch を見ればいいか」の
ポインタ**に限ります。branch 固有の詳細（何が途中か、次に何をするか）はその branch 自身の
コミットや開いてある PR の本文に書き、ここには短いポインタだけを置いてください。恒久的な
リポジトリのルールもここには書きません（`AGENTS.md`・`docs/decisions.md` を参照）。

同時に複数 branch が進行中の場合は、「次にやること」に branch ごと1エントリで列挙してください。
この雛形は複数 branch の並行開発を想定した状態管理は持っていません。本格的に必要になったら
`docs/decisions.md`「3-b. セッション間の引継ぎ」を読んでから設計し直してください。

- `AGENTS.md` がこのファイルを `@` で import しているため、セッション開始時に自動で読み込まれます
- 未記録の変更が残っていると、`.claude/hooks/handoff-check.sh` が1セッションに1回だけ更新を求めます
- セッション終了時、`.claude/hooks/handoff-stamp.sh` が末尾の状態を機械的に**上書き**します（追記ではない）
- 作業開始時は `/checkin`、区切りでは `/handoff`、終了時は `/checkout` を実行してください

**このファイル（引継ぎ文）を更新したら、必ず `main` へマージしてください。** 次のセッションは
`main` を新規クローンし、この docs/handoff.md しか自動では読みません。実際のコード変更は、
未完了なら push 済みの branch/worktree に残したままで構いません（push していれば origin に
残るため消えません）。

---

## いま何をしているか

Graph Engineering（ユーザー提示の外部研究資料に基づく、複数AIが並行して大計画のゴールへ
向かう運用）の基礎作りが進行中。失敗行動台帳・放置台帳は実装・mainマージ済み。Task Graph
（計画の構造・番号）は設計討議が大きく進み、3視点による敵対的検証と一次情報調査まで完了したが、
**未実装**。見つかった4つの欠陥への対処案は一次情報の裏付き
で出そろっており、ユーザーの確認待ち。次セッションはその確認から始める。

## 完了したこと

PR #1〜#30 をすべてマージ済み。うち直近の主なもの:

- **`docs/failure-action-log.md`（失敗行動台帳）を追加**（PR #27, #28）— 失敗したと気づいた
  その場で本人が書く。**結果ではなく行動を書く**（結果は`test-policy.md`とテスト・CIの担当
  領域）。書きっぱなしでよい。通常は読まない。読むのはユーザーの明示的指示、または行き詰まって
  何が悪いか分からないとき（後者は読む前に必ずユーザーへ確認）。読むときは`failure-reviewer`
  サブエージェントに委譲し、今の状況と一致する記録を探し、無ければ「該当なし」と報告する
  （正解・戦略は提案しない）。存在理由は「正解を探すためではなく、確実な失敗行動を除外して
  選択肢を絞るため」の消去法
- **`docs/neglected-log.md`（放置台帳）を追加・確定**（PR #27, #29, #30）— `test-policy.md`の
  Backlog Rule（Gate 081〜100の未修正記録）と統合した。**書くきっかけは「Gateに触れたが破ら
  なかった（ヒヤリハット）」に限定**。書く内容は「日時・触れたGate番号/カテゴリ・対象箇所」の
  3点のみで、理由は書かせない（読み手が推測せず実際に確認しに行く設計）。大計画のゴール
  （問題がなければ即リリースできる状態）到達時にのみ読む
- **`AGENTS.md`に「非エンジニア向けに説明する」を復活**（PR #26）— PR #24の分量削減で一度
  削っていたが、「都度指示させるのは非効率。理解できないまま指示・判断させると作業ミスを
  生む」というユーザー指摘を受け、判断を改めて復活させた
- **Task Graph（計画の構造・番号）の設計討議**（今回、未実装）— ライン（依存関係テストのみで
  分割）、フェーズ・ステップ番号（ライン内で独立カウント）、寄り道（最大9、定義しない）、
  進捗表示（ラインごと＋全体合計、寄り道は別枠）、依存関係の記述（成果物を文章で明記）、
  3視点（技術的／業務フロー／リスク）の敵対的検証、まで具体的に合意した。この設計案を実際に
  3視点AIで敵対的検証したところ、4つの重大な欠陥（①並列編集時の整合性未設計 ②再採番で
  過去の番号参照が壊れる ③計画者＝実行者＝完了判定者が同一AIで自己申告を検証する仕組みがない
  ④「寄り道の定義を決めない」ことが抜け道になりうる）が見つかった。詳細は
  `docs/decisions.md`「9. 失敗行動台帳・放置台帳」内「保留にした部分：Task Graph」を参照
- **Task Graphの4つの欠陥への対処案を、一次情報の裏付きでまとめた**（今回、未実装・未合意）—
  「4つの欠陥を個別に潰すのではなく、計画の立て方／立てるタイミング／全体計画とセッション実行
  計画の境界／セッション粒度を先に確定すれば解決できる」というユーザーの指摘を起点に、公式
  ドキュメントを調査した。Anthropic「Effective harnesses for long-running agents」と GitHub
  spec-kit が同じ3層構造（恒久ルール／書き換えない全体計画／毎回作り直す実行計画）に収束して
  いることを確認。原文根拠・設計変更案・捨てることになる項目を `docs/decisions.md` の同節に
  「一次情報の調査結果（2026-08-30）」以下として追記済み

## 次にやること

1. **Task Graphの設計変更案をユーザーに確認してもらってから実装する。** 案の全文と原文根拠は
   `docs/decisions.md`「9. 失敗行動台帳・放置台帳」内「保留にした部分：Task Graph」の
   「一次情報の調査結果（2026-08-30）」以下に記録済み。まだブランチは切っていない
   （設計討議のみでコード変更なし）。**ユーザーに判断してもらう必要がある未決の点は4つ**:
   - 全体計画のファイル形式を JSON（書き換えにくいが読みにくい）にするか Markdown のコード
     ブロック（読みやすいが書き換えられやすい）にするか
   - 完了判定を `/checkout` に組み込むか、独立したサブエージェントにするか
   - 「依存関係テストのみで分割、ファイル重複は基準にしない」という既存の合意を、公式に
     合わせて「ファイル重複も並列判定に入れる」へ変更してよいか
   - 1つの全体計画に項目をいくつ置くか（Anthropic の実例は200項目以上）
2. **`Use this template` で新規リポジトリを1本作り、実地検証する。**（PR #13 時点からの
   積み残しタスク。まだ未着手）確認する項目:
   - `pnpm install` → `pnpm run check` → `pnpm run build` が通る
   - `/context` で `CLAUDE.md` が Memory files に出る
   - `.ts` を編集すると Prettier と `tsc` のフックが走る
   - workspace trust を承認する前後で `permissions.allow` の効き方が変わる
3. GitHub Ruleset の実態は `[曖昧]` のまま。ユーザーが GitHub の Settings → Rules → Rulesets
   画面で確認し、`AGENTS.md` の記述と食い違いがあれば知らせてほしい
4. `handoff-stamp.sh`（SessionEnd）は実際にセッションを終了させないと harness 経由で発火
   させられない。手動 JSON テストのみで確認済み（優先度は低い）

## 注意点

**恒久的なリポジトリのルールはここに書かない。** `AGENTS.md`（指示）と `docs/decisions.md`
（根拠）を参照する。ここに書くのは、セッションをまたいで再発しうる作業上の落とし穴だけ。

- **設計討議だけで終わったセッションでも、合意内容を`docs/decisions.md`に記録してから
  チェックアウトする。** 記録しないと、会話ログにしか残らない設計が次セッションから完全に
  見えなくなる（今回のTask Graph設計がまさにこれに該当したため、忘れずに記録した）
- **設計を議論するときは、抽象的な原則だけでなく具体例（実際のファイルの中身、セッションの
  流れ）まで書く。** 今回、原則の一覧を提示した段階でユーザーから「例が無いと分からない」と
  差し戻された。`AGENTS.md` の「非エンジニア向けに説明する」は、用語を噛み砕くだけでなく
  具体例を出すことまで含む
- **複数AIが同時に同じ計画ファイルを編集するときの排他制御は、公式・OSSどの一次情報にも
  記述がない `[曖昧]`。** spec-kit は「別ファイルのタスクだけ並列可」にしてそもそも競合させ
  ない方向で回避しており、ロック機構を持たない。探しても見つからないので再調査は不要
- 公式の `settings` ページだけは全文を読めていない（サイズ超過）。設定キーの一覧は
  `settings-reference` の索引表が根拠（`docs/decisions.md` の「8. 調査の範囲と限界」に記載）

<!-- session-end-stamp -->

## セッション終了時点の状態（自動記録）

- 記録時刻: 2026-08-30 00:59 UTC
- ブランチ: `claude/checkin-pr7h9s`
- HEAD: `3d89641`
- 未コミットの変更:

```
M docs/handoff.md
```
