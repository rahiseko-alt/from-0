# 引継ぎ

セッションをまたいで作業を継続するための文書です。役割は**「次にどの branch を見ればいいか」の
ポインタ**に限ります。branch 固有の詳細（何が途中か、次に何をするか）はその branch 自身の
コミットや開いてある PR の本文に書き、ここには短いポインタだけを置いてください。恒久的な
リポジトリのルールもここには書きません（`AGENTS.md`・`docs/decisions.md` を参照）。

同時に複数 branch が進行中の場合は、「次にやること」に branch ごと1エントリで列挙してください。
この雛形は複数 branch の並行開発を想定した状態管理は持っていません。本格的に必要になったら
`docs/decisions.md`「3-b. セッション間の引継ぎ」を読んでから設計し直してください。

- `AGENTS.md` がこのファイルを `@` で import しているため、セッション開始時に自動で読み込まれます
- 未記録の変更が残っていると、`.claude/hooks/handoff-check.sh` が1セッションに1回だけ更新を求めます
- セッション終了時、`.claude/hooks/handoff-stamp.sh` が末尾の状態を機械的に**上書き**します（追記ではない）
- 作業開始時は `/checkin`、区切りでは `/handoff`、終了時は `/checkout` を実行してください

**このファイル（引継ぎ文）を更新したら、必ず `main` へマージしてください。** 次のセッションは
`main` を新規クローンし、この docs/handoff.md しか自動では読みません。実際のコード変更は、
未完了なら push 済みの branch/worktree に残したままで構いません（push していれば origin に
残るため消えません）。

---

## いま何をしているか

**この雛形の全数監査が終わったところ。次セッションは「配線」の実装から始める。**

雛形の目的が言語化された（ユーザー確定）。

- **目的**: 非エンジニアがサクサク安心して開発が進む仕組み
- **手段**: 並列ネイティブ／機械検証／進捗管理／放置と振り返りの仕組み／非エンジニア向け説明ルール
- **構成**: 土台のみで構成する
- **制約**: **上物（プロジェクト固有）で土台（雛形共通）が変わってはならない。変えてよいのは
  ユーザーの明示指示があるときだけで、AI の判断で変えてはいけない**

この目的に照らして実装を全数監査した結果、**致命的な欠陥27件**。ただし
**「部品が無い」のではなく「部品はあるが、どこからも呼ばれていない」**のが実態。
実際に配線されているのは `validatePlan`（CI 経由）**1つだけ**。

全文と根拠は `docs/decisions.md`「13. 雛形の目的の言語化と、実装の全数監査」。
**次セッションは着手前にこの節を必ず読むこと。監査をやり直す必要はない。**

## 完了したこと

PR #1〜#36 をマージ済み。`main` は `425d082`。

- **雛形の目的を言語化し、実装を全数監査した**（今回）— サブエージェント3体で並列調査。
  結果は `docs/decisions.md` の「13.」に A（配線するだけ）/ B（部品が無い）/ C（部品が浅い）
  の3分類で記録済み
- **PR #35 を revert した**（PR #36）— 技術構成の分離と `/project-init` を入れたが、
  `/project-init` が `AGENTS.md`（土台の中心）を書き換える設計で、上記の制約に違反していた。
  **同じ過ちを繰り返さないこと。目的・技術構成は土台の外のファイルに置く**
- **全体計画の仕組み**（PR #33）— `src/plan.ts` / `docs/plan.example.json` /
  `.claude/agents/completion-checker.md` / `.claude/skills/plan-init`・`plan-verify`・`plan-ask`。
  **部品としては動くが、`validatePlan` 以外は呼び出し元が無い**
- **失敗行動台帳・放置台帳**（PR #27〜#30）— **導入以降エントリ0件。書くきっかけが文章だけ**

## 次にやること

**A → B → C の順。A が最も割に合う。**

### 1. A: 既にある部品に呼び出し元を作る（次セッションはここから）

すべて `src/plan.ts` に実装済みで、テスト以外から呼ばれていない。

- `countProgress` → **進捗を表示するコマンドを作る。** 非エンジニアが現在地を知る手段が
  いま1つも無い（AI に口頭で聞くしかない）。これが最優先
- `nextItem` → `/checkin` が実際に呼んで「次の1件」を出す
- `nextId` → 追記時の採番を機械にやらせる（いま AI の手作業。並列時に番号が衝突する）
- `canRunInParallel` → 並列可能な項目を列挙する手段を作る
- `completion-checker` / `failure-reviewer` → 呼ぶきっかけを文章以外にできないか検討

**注意**：`src/plan.ts` は TypeScript なので、この配線をどこに置くかで
「土台が上物の言語に依存する」問題に再び当たる。**先に置き場所を決めてから実装すること。**

### 2. B: 存在しない部品を作る

- **ゴール到達の検出**。これが無いため `docs/neglected-log.md` は読まれる契機が永久に来ない
- **「前回の照合時点の済数」の記録場所**。`/checkin` の「10項目ごとに `/plan-verify`」が
  比較対象を持たず発火しない
- **非エンジニア保護の強制**。フック4本はいずれも発話・質問文を検査していない。
  実例：このセッションで AI が「PR #35 を revert しますか？ y/n」と聞き、ユーザーから
  「非エンジニアです分かりません」と返された。**どのルールにも違反していなかった**
  （聞き方の制約が `/plan-ask` の中にしか書かれていないため）
- **並列で実際に走らせる仕組み**。`git worktree` を作る箇所がリポジトリ全体でゼロ。
  「並列ネイティブ」の根拠が文章と未使用の関数しかない

### 3. C: 浅い部品を作り直す

- `verify` の検証が**空配列かどうかしか見ていない**（`src/plan.ts:125`）
- 番号の不変性が**並び替えしか見ていない**（`src/plan.ts:108`）。前バージョンと比較しないため
  削除・全書き換え・振り直しが素通り
- `docs/plan.json` が無いとテストごとスキップ（`src/plan.file.test.ts:42`）。消しても緑

### 4. 土台への上物混入を剥がす（PR #35 の作り直し）

**制約を守る形で。** 混入箇所の一覧は `docs/decisions.md`「13.」にある。
`AGENTS.md` 約50行、`.claude/settings.json` の `pnpm install`、
`format.sh`/`typecheck.sh`、各スキルの `pnpm run *`、`README.md` 全文、`package.json` の名前。

### 5. 以前からの積み残し

- GitHub Ruleset の実態は `[曖昧]`。ユーザーが Settings → Rules → Rulesets で確認してほしい
- `handoff-stamp.sh`（SessionEnd）の実発火は未確認（優先度は低い）

## 注意点

**恒久的なリポジトリのルールはここに書かない。** `AGENTS.md`（指示）と `docs/decisions.md`
（根拠）を参照する。ここに書くのは、セッションをまたいで再発しうる作業上の落とし穴だけ。

- **土台（`AGENTS.md`・`.claude/` 配下）をプロジェクトごとに書き換える設計にしないこと。**
  PR #35 がこれで revert になった。プロジェクト固有のものは土台の外のファイルに置き、
  土台はそれを読むだけにする
- **非エンジニアに答えられない質問をしない。** GitHub の操作、CI の赤、コンフリクト、
  revert の可否は、そのまま聞いても答えられない。**何が起きているか・推奨はどれか・
  選ぶとどうなるかを平易に書いてから、選択肢で聞く**
- **大げさに言わない。** 監査結果を報告する際、AI 側が「仕組みが無い」と言い切ったが、
  実際は「部品はあるが配線されていない」だった。ユーザーに指摘されて修正した
- **設計討議だけで終わったセッションでも、合意内容を `docs/decisions.md` に記録してから
  チェックアウトする**
- **設計を議論するときは具体例（実際のファイルの中身、セッションの流れ）まで書く**
- **複数AIが同時に計画ファイルを編集するときの排他制御は、一次情報に記述がない `[曖昧]`。**
  spec-kit も持たない。探しても見つからないので再調査は不要
- 公式の `settings` ページだけは全文を読めていない（サイズ超過）

<!-- session-end-stamp -->

## セッション終了時点の状態（自動記録）

- 記録時刻: 2026-08-30 11:04 UTC
- ブランチ: `claude/checkin-pr7h9s`
- HEAD: `4529541`
- 未コミットの変更: なし
