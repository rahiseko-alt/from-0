# 引継ぎ

セッションをまたいで作業を継続するための文書です。役割は**「次にどの branch を見ればいいか」の
ポインタ**に限ります。branch 固有の詳細（何が途中か、次に何をするか）はその branch 自身の
コミットや開いてある PR の本文に書き、ここには短いポインタだけを置いてください。恒久的な
リポジトリのルールもここには書きません（`AGENTS.md`・`docs/decisions.md` を参照）。

同時に複数 branch が進行中の場合は、「次にやること」に branch ごと1エントリで列挙してください。
この雛形は複数 branch の並行開発を想定した状態管理は持っていません。本格的に必要になったら
`docs/decisions.md`「3-b. セッション間の引継ぎ」を読んでから設計し直してください。

- `AGENTS.md` がこのファイルを `@` で import しているため、セッション開始時に自動で読み込まれます
- 未記録の変更が残っていると、`.claude/hooks/handoff-check.sh` が1セッションに1回だけ更新を求めます
- セッション終了時、`.claude/hooks/handoff-stamp.sh` が末尾の状態を機械的に**上書き**します（追記ではない）
- 作業開始時は `/checkin`、区切りでは `/handoff`、終了時は `/checkout` を実行してください

**このファイル（引継ぎ文）を更新したら、必ず `main` へマージしてください。** 次のセッションは
`main` を新規クローンし、この docs/handoff.md しか自動では読みません。実際のコード変更は、
未完了なら push 済みの branch/worktree に残したままで構いません（push していれば origin に
残るため消えません）。

---

## いま何をしているか

**`docs/plan.json` 26項目のうち20項目が完了。残り6件は、どれも人の操作か判断が要る。**

`pnpm run plan:progress` を実行すれば、いつでも今の数字が出る。

## 完了したこと

作業ブランチ `claude/checkin-24gv3k`。`main` は PR #41（`T001`）をマージ済み。

- **計画を読む CLI を4本追加**（`T002` `T011` `T017` `T027` とそのテスト `T003` `T012`）
  - `pnpm run plan:progress` 進み具合／`plan:next` 次の1件／`plan:next-id` 次の番号／
    `plan:parallel` 同時に進めてよい組
  - 第1引数で別の計画ファイルを渡せる（テスト用の計画で試せる）
- **進捗表示から専門用語を無くした**（`T013`）— 「全26件のうち20件が終わりました」の形
- **`verify` の書き方を機械で検査するようにした**（`T009` `T010`）—「テストが通る」のように
  何を実行し何が起きれば合格かを示さない書き方を `validatePlan` が拒む。判定は項目ごと
  （下ごしらえの手順に期待結果が無いのは正当なため）
- **フックを2本追加して `.claude/settings.json` に登録**（`T022` `T023` `T026` の実装）
  - `neglect-check.sh` Gate に触れた形跡があるのに放置台帳が空なら差し戻す
  - `question-style-check.sh` 選択肢の無い質問・説明の無い専門用語を差し戻す
  - どちらも1セッションに1回だけ。`handoff-check.sh` も台帳の未記入を指摘するようにした
- **`/checkin` を書き換えた**（`T004` `T015`）— 次の1件を目視で選ばず `plan:next` に選ばせ、
  `plan:parallel` の組があればユーザーに聞く
- **`AGENTS.md` に追記**（`T001` `T006` `T016` `T018` `T024`）— 並列で進める手順、`/goal` の
  宣言、番号は `plan:next-id`、質問は `AskUserQuestion`、週次 Routine の読み方
- **設計メモを2本**（`T021` `T025`）— `docs/decisions.md`「18.」Gate 抵触検出、
  「19.」質問スタイル検査。「20.」に Routine の設計
- **週次 Routine を作成**（`T018`）— 毎週月曜 00:00 UTC。進み具合と見送り件数を通知する
- **完了判定を独立したサブエージェント5本に受けた**（`T007`）— `status` も git 履歴も
  見せず `verify` だけをなぞらせた。記録は `.claude/skills/checkout/SKILL.md` の手順0
- `pnpm run check` / `pnpm run build` 通過（46テスト）

## 次にやること

**残り6件は、どれも人の操作か判断が要る。AI だけでは進められない。**

| 項目   | 何が要るか                                                                                                  |
| ------ | ----------------------------------------------------------------------------------------------------------- |
| `T005` | `Workflow` の `parallel()` を実際に走らせる。**ユーザーが明示的に頼まないと呼べない**ツールのため、依頼待ち |
| `T006` | `/goal` が「満たした」と判定する場面の観測。判定役のセッションからは `/goal` を起動できない                 |
| `T014` | 実在の非エンジニアに `pnpm run plan:progress` の画面を見せて、進み具合を説明してもらう                      |
| `T019` | 作成した Routine を Run now で実行し、通知が届くのを目視する                                                |
| `T020` | 全項目が終わった状態で Routine を実行し、通知文言がゴール到達を示すか見る（`T019` の後）                    |
| `T026` | 質問スタイル検査フックの実地確認。実装と登録は済んでおり、`automation` が `human` のため人の確認待ち        |

- `T014` `T019` `T020` `T026` は `awaiting_human`。**`/plan-ask` でまとめて聞くこと。1件ずつ聞かない**
- `T005` `T006` は `todo` のまま（判定役が「通らなかった」と判定、または観測できなかったため）

### 以前からの積み残し

- GitHub Ruleset の実態は `[曖昧]`。ユーザーが Settings → Rules → Rulesets で確認してほしい
- `handoff-stamp.sh`（SessionEnd）の実発火は未確認（優先度は低い）

## 注意点

**恒久的なリポジトリのルールはここに書かない。** `AGENTS.md`（指示）と `docs/decisions.md`
（根拠）を参照する。ここに書くのは、セッションをまたいで再発しうる作業上の落とし穴だけ。

- **判定役に渡す記録には必ず対象の id を書く。** 「通らなかった項目は据え置いた」とだけ
  書いた記録は、判定役から「`status` を見ない制約では裏が取れない」と差し戻された
- **ドキュメントに具体例を載せるときは、その例を実際に実行・検証してから載せる。**
  もっともらしい例は、検証しないと自分の書いた条件に違反していても気づけない
  （実際に `AGENTS.md` の並列の例がそうなっていた）
- **`node -e` の中でトップレベル `return` は書けない**（`SyntaxError: Illegal return
statement`）。フックの中で `node -e` を使うときは関数で包むこと。`2>/dev/null` を
  付けていると、この失敗が黙って握り潰される
- **`docs/plan.json` の項目を自分で「済」にしない。** `/checkout` の手順0で
  `completion-checker` に判定させてから `status` を変える
- **下調べや設計判断の成果は新しいファイルを作らず `docs/decisions.md` に追記する**（ユーザー指示）
- **`AGENTS.md` が 200 行の目安を 23 行超えている**（223行）。逸脱の理由と戻してよい条件は
  `docs/decisions.md`「17.」
- **`git push` がプロキシの 503（CONNECT tunnel failed）で失敗することがある。**
  そのまま再実行すれば通った。指数バックオフで数回リトライする
- **`.claude/hooks/stop-hook-git-check.sh` 相当のフックが、`docs/handoff.md` 末尾の
  自動スタンプ（`<!-- session-end-stamp -->` 以降）を書き換えるたびに「未コミットの変更」
  として検出することがある。** 中身が記録時刻/HEADの更新だけなら `git checkout --
docs/handoff.md` で戻してよい
- 公式の `settings` ページだけは全文を読めていない（サイズ超過）

<!-- session-end-stamp -->` 以降）を書き換えるたびに「未コミットの変更」

として検出することがある。** 中身が記録時刻/HEADの更新だけなら `git checkout --
docs/handoff.md` で戻してよい（実質的な引継ぎ内容の変更ではないため）

- 公式の `settings` ページだけは全文を読めていない（サイズ超過）

<!-- session-end-stamp -->` 以降）を書き換えるたびに「未コミットの変更」

として検出することがある。** 中身が記録時刻/HEADの更新だけなら `git checkout --
docs/handoff.md` で戻してよい（実質的な引継ぎ内容の変更ではないため）

- 公式の `settings` ページだけは全文を読めていない（サイズ超過）

<!-- session-end-stamp -->

## セッション終了時点の状態（自動記録）

- 記録時刻: 2026-08-31 14:20 UTC
- ブランチ: `claude/checkin-j8rytv`
- HEAD: `62f15bb`
- 未コミットの変更: なし
