# 引継ぎ

セッションをまたいで作業を継続するための文書です。役割は**「次にどの branch を見ればいいか」の
ポインタ**に限ります。branch 固有の詳細（何が途中か、次に何をするか）はその branch 自身の
コミットや開いてある PR の本文に書き、ここには短いポインタだけを置いてください。恒久的な
リポジトリのルールもここには書きません（`AGENTS.md`・`docs/decisions.md` を参照）。

同時に複数 branch が進行中の場合は、「次にやること」に branch ごと1エントリで列挙してください。
この雛形は複数 branch の並行開発を想定した状態管理は持っていません。本格的に必要になったら
`docs/decisions.md`「3-b. セッション間の引継ぎ」を読んでから設計し直してください。

- `AGENTS.md` がこのファイルを `@` で import しているため、セッション開始時に自動で読み込まれます
- 未記録の変更が残っていると、`.claude/hooks/handoff-check.sh` が1セッションに1回だけ更新を求めます
- セッション終了時、`.claude/hooks/handoff-stamp.sh` が末尾の状態を機械的に**上書き**します（追記ではない）
- 作業開始時は `/checkin`、区切りでは `/handoff`、終了時は `/checkout` を実行してください

**このファイル（引継ぎ文）を更新したら、必ず `main` へマージしてください。** 次のセッションは
`main` を新規クローンし、この docs/handoff.md しか自動では読みません。実際のコード変更は、
未完了なら push 済みの branch/worktree に残したままで構いません（push していれば origin に
残るため消えません）。

---

## いま何をしているか

**`from-0` 自身の `docs/plan.json`（全体計画）を作り終えた。まだ1項目も着手していない。
次セッションは `/checkin` で1件選んで実装に入ってよい。**

前回セッションで5手段の設計討議・公式機能の採否（`docs/decisions.md`「14.」「15.」）が
確定した。今回はユーザーの指示で、その「15.」の表を実際に `docs/plan.json` に落とした。

- 最初に10行→12項目でざっくり作ったが、ユーザーから「イシューの粒度まで分解しろ」と
  指示があり、26項目まで分割し直した
- **分割した本人が粒度を判定しない**ため、独立サブエージェントに品質レビューを依頼した。
  指摘（重複項目・依存の向き違い・フック登録漏れ・`automation`分類の誤りなど）は全て反映済み
- 「実装手順そのもの（実行計画）まで書き出すべきでは」という指摘もあったが、
  `AGENTS.md`53行目「セッションごとの実行計画はファイルに残さない」という既存方針を
  根拠つきで提示して反論し、ユーザーが採用した。**26項目より先の分解はしない**
- `pnpm run test` で `validatePlan` を通過済み。全項目 `status: todo`（未着手）

## 完了したこと

`main` は `27fcc92`。作業ブランチ `claude/checkin-j8rytv` に5コミット push 済み
（`bc971be` 下調べ記録、`2a26f5b` 採否判断、`ac1b3e6` plan.json 初版、`54c66a6`
レビュー反映、`62f15bb` 実行計画非永続化の再確認）。PR はまだ出していない
（下記「次にやること」参照）。

- **`docs/plan.json` を作成した**（今回）— `docs/decisions.md`「15.」の10行を26項目に
  分解。①並列ネイティブ5件・②機械検証4件・③進捗管理7件・④放置と振り返り6件・
  ⑤非エンジニア向け説明ルール3件、プラス`nextItem`用CLI1件
- **サブエージェントによる独立レビューを実施し、指摘を反映した**（今回）— 経緯は
  `docs/decisions.md`「16.」
- **5手段すべての採否を部品単位で確定**（前回）— `docs/decisions.md`「15.」
- **雛形の目的を言語化し、実装を全数監査した**（数回前）— `docs/decisions.md`「13.」

## 次にやること

**`docs/plan.json` から1件選んで実装する。1セッション1件。**

- 依存先が無く今すぐ着手できる項目: `T001, T002, T006, T007, T009, T011, T017, T021,
T024, T025, T027`（11件）
- 非エンジニアへの効果が一番大きいのは `T011`（進捗表示コマンド）系の一連
  （`T011→T012/T013→T014`、`T015`・`T027` もこれに続く）
- `/checkin` の「次の1件を選ぶ」は、今はまだ `nextItem` 関数を呼んでおらず文章の指示の
  ままなので、次セッションは手作業で `docs/plan.json` を見て選ぶこと（`T015` がこれ自体を
  直す項目）
- **PRはまだ出していない。** 今回のセッションはここでチェックアウトするが、
  実装を始める前に一度 `main` にこの引継ぎ内容を取り込む

### 以前からの積み残し

- GitHub Ruleset の実態は `[曖昧]`。ユーザーが Settings → Rules → Rulesets で確認してほしい
- `handoff-stamp.sh`（SessionEnd）の実発火は未確認（優先度は低い）

## 注意点

**恒久的なリポジトリのルールはここに書かない。** `AGENTS.md`（指示）と `docs/decisions.md`
（根拠）を参照する。ここに書くのは、セッションをまたいで再発しうる作業上の落とし穴だけ。

- **`docs/plan.json` の項目を自分で「済」にしない。** `/checkout` の手順0（未着手のため
  今回は該当なし）を次回から必ず踏むこと
- **下調べや設計判断の成果は新しいファイルを作らず `docs/decisions.md` に追記する**
  （ユーザー指示）。今回も「16.」に追記した
- **計画の粒度を自分で判定しない。** 分割した本人が「これで十分」と自己評価すると
  甘くなる。今回のように独立サブエージェントに検証させること
- **非エンジニアに答えられない質問をしない。** 何が起きているか・推奨はどれか・選ぶと
  どうなるかを平易に書いてから、選択肢で聞く
- **設計討議だけで終わったセッションでも、合意内容を `docs/decisions.md` に記録してから
  チェックアウトする**
- **`.claude/hooks/stop-hook-git-check.sh` 相当のフックが、`docs/handoff.md` 末尾の
  自動スタンプ（`<!-- session-end-stamp -->` 以降）を書き換えるたびに「未コミットの変更」
  として検出することがある。** 中身が記録時刻/HEADの更新だけなら `git checkout --
docs/handoff.md` で戻してよい（実質的な引継ぎ内容の変更ではないため）
- 公式の `settings` ページだけは全文を読めていない（サイズ超過）

<!-- session-end-stamp -->

## セッション終了時点の状態（自動記録）

- 記録時刻: 2026-08-31 14:20 UTC
- ブランチ: `claude/checkin-j8rytv`
- HEAD: `62f15bb`
- 未コミットの変更: なし
