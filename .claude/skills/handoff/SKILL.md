---
name: handoff
description: docs/handoff.md をいまの状態に更新してコミットする。作業の区切りや、コンテキストが逼迫してきたときに使う。作業を終えるときは checkout を使うこと。
---

# 引継ぎの更新（セッション途中）

`docs/handoff.md` を現時点の状態に更新してコミットします。

> **作業を終えるときはこのスキルではなく `/checkout` を使ってください。**
> `/checkout` は `main` へのマージまで面倒を見ます。このスキルはコミットまでです。
> コミットだけでは作業ブランチに留まり、コンテナ破棄とともに失われます。

## 手順

1. `docs/handoff.md` を読み、現状との差分を把握する
2. `git status` と `git log --oneline -10` で、このセッションの変更を確認する
3. 下記の4節を書き換える
4. `docs/handoff.md` をコミットする

## 書く内容

**いま何をしているか**
目的と、どこまで進んだか。作業が完了しているなら「完了済み。次の作業は未着手」と書く。

**完了したこと**
変更したファイル、出した PR の番号とリンク、通した検証。「動くはず」ではなく、実際に確認した事実だけを書く。

**次にやること**
具体的な次の一手。判断待ちがあるなら、誰の何の判断を待っているかを書く。複数 branch が
同時進行中なら、branch ごと1エントリで列挙する（詳細はその branch のコミット/PR に置く）。

**注意点**
踏んだ落とし穴、未解決の問題、**試して駄目だった方法**。同じ失敗を繰り返させないため、駄目だった方法は特に価値がある。

## 書かないこと

- 経緯や検討の過程。結論と次の一手だけでよい
- コードを読めば分かること。ファイルの一覧や関数の説明は不要
- 一時的な情報。次のセッションで無意味になるものは書かない
- **`AGENTS.md`/`docs/decisions.md` に既にある恒久的なリポジトリのルール。** 二重管理は
  食い違いの元になる。参照させるだけにする
- **進み具合の数字。** `pnpm run plan:progress` が数えて出すので、書くと必ず古くなる
- **いまの工程。** `pnpm run plan:state` が判定するので、書くと必ず古くなる

確度の低い記述には `[曖昧]` を付けてください。

## セッション終了時の自動記録について

`.claude/hooks/handoff-stamp.sh` は、ブランチ・HEAD・未コミットの変更を
**`.claude/.session-end.md`（gitignore 済み・手元だけ）** に書きます。
`docs/handoff.md` には触れません。

以前はこの内容を `docs/handoff.md` の末尾に直接書いていましたが、SessionEnd の後に
commit も push も行われないため、その追記は origin に届きませんでした。届かない情報を
引継ぎ文に混ぜると「書いてあるのに次のセッションからは見えない」食い違いになります。
**永続化は `/checkout`（commit して main へマージするところまで）の責任です。**
