---
name: checkin
description: 作業開始時の点検。main を同期し、引継ぎと未処理の PR を確認して、次の一手を提示する。ユーザーが作業を始めるとき、セッションの最初に使う。
---

# チェックイン

作業を始める前の点検です。**報告は「現在地」と「次の一手」だけ**にしてください。

## 手順

0. **未初期化を検出する**

   ```bash
   grep -c "このリポジトリを雛形として使い始めたら" AGENTS.md
   ```

   **0が返れば初期化済みです。何も言わずに次へ進んでください。**

   **1以上なら、目的が未設定です。** ただしこのとき、次の2つは**中身が完全に同じで、
   機械的には区別できません。**

   - 雛形リポジトリそのもの（雛形を開発している）
   - 雛形をコピーした直後のプロジェクト（目的がまだ決まっていない）

   コピーは中身が1バイトも変わらないので、これは原理的に判定できません。**ユーザーに
   1回だけ聞いてください。**

   ```
   このリポジトリの目的は何ですか？

     AGENTS.md の目的欄が雛形の既定のままです。次のどちらですか。

     [ 1 ] 雛形リポジトリそのもの（雛形自体を開発している）
           → このまま続けます
     [ 2 ] 雛形をコピーして始めるプロジェクト
           → /project-init で目的と技術構成を決めます
   ```

   **1** と答えられたらそのままチェックインを続けます。
   **2** なら、チェックインを中断して `/project-init` を実行します。

   **推測で決めないでください。** リポジトリ名やコードから「たぶんコピーだろう」と
   判断して勝手に進めると、雛形自身の開発中に目的を書き換えてしまいます。

1. **main を同期する**

   ```bash
   git fetch origin main --prune
   git status --short
   git log --oneline origin/main -3
   ```

   ローカルが `origin/main` より古い場合は追いつかせます。作業ブランチが前回のマージで
   不要になっているなら、`origin/main` から作り直します。

2. **引継ぎを読む**

   `docs/handoff.md` は `AGENTS.md` から import されるため、すでにコンテキストにあります。
   ただし**それは前回セッション終了時点の内容**です。`origin/main` の最新版と食い違っていないか、
   ファイルを開いて確認してください。

3. **未処理の PR を確認する**

   オープンな PR があれば、CI の結果とマージ可否、未解決のレビューコメントを確認します。
   赤い CI やコンフリクトが残っていれば、それが最優先の作業です。

4. **次の1項目を選ぶ**（`docs/plan.json` がある場合のみ）

   未着手（`todo`）のうち、依存先が全て完了している最初の項目を1つだけ選びます。
   **1セッションで扱うのは1項目だけです。** 一気に進めようとするとコンテキストが尽きます。

   着手する前に、前回までの成果が**今も動くか**をざっと確かめてください。いきなり実装に
   入ると、既に壊れているものの上に積み増して問題を悪化させます。

   「済」の項目が前回の照合から10増えていたら、`/plan-verify`（全体照合）を先に走らせます。

5. **食い違いを検出する**

   引継ぎの「次にやること」が、すでに完了している／もう不要になっていることがあります。
   その場合は指摘してください。黙って古い計画を実行しないこと。

## 報告の形式

```
現在地: <いま何がどこまで進んでいるか。1〜2行>
未処理: <オープンな PR、赤い CI、コンフリクト。無ければ「なし」>
スコープ: <このセッションでどこまでやって終わりにするか。完了条件を1行で>
次の一手: <具体的に何をするか。1行>
```

引継ぎと実態が食い違っていた場合のみ、その差分を追記してください。

### スコープの決め方

**「終わったと判断できる線」を、着手前に引いてください。** 線を引かずに始めると、
どこまでやっても終わらず、コンテキストが尽きた時点で中途半端に切れます。

- `docs/plan.json` がある場合 — 選んだ1項目がそのままスコープです
- 無い場合 — ユーザーの依頼を「何ができたら終わりか」の形に言い換えます

```
✗  スコープ: 認証まわりを改善する
✓  スコープ: ログイン画面でパスワード再設定リンクを押すとメールが届く状態まで
```

**スコープが1セッションに収まらないと判断したら、その場で言ってください。** 縮める判断は
ユーザーのものです。黙って一部だけやって「完了」と報告しないこと。

## スコープを終えたら

**スコープを達成したら、ユーザーに聞かずに `/checkout` を実行してください。**
「終わりました。チェックアウトしますか？」と聞く必要はありません。

引継ぎを `main` にマージするところまでが `/checkout` です。マージしないまま
セッションが終わると、このセッションの成果は次のセッションから見えなくなります。

例外は次の2つだけです。この場合は `/checkout` せず、状況を報告して指示を仰ぎます。

- スコープの途中でユーザーの判断が必要になり、答えを待っている
- スコープを達成できないことが分かった（原因を明示すること）
